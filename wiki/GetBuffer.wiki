#summary Read byte in buffer
#labels Phase-Implementation

= Introduction =

Used to read the first byte in the buffer.


= Details =

Return `Buffer[0]` and remove the byte so the next time the next byte is read.